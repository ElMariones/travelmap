name: TravelMap
options:
  bundleIdPrefix: com.placeholder
  deploymentTarget:
    # Liquid Glass (glassEffect, GlassEffectContainer, the glass button styles) is
    # iOS 26 only. There is no back-deployment for it, so adopting the design system
    # sets the floor.
    iOS: "26.0"
  createIntermediateGroups: true
  groupSortPosition: none

configs:
  Debug: debug
  Release: release

settings:
  base:
    SWIFT_VERSION: "5.0"
    SWIFT_STRICT_CONCURRENCY: complete
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic
    ENABLE_USER_SCRIPT_SANDBOXING: YES

packages:
  Supabase:
    url: https://github.com/supabase/supabase-swift
    from: 2.55.1

targets:
  TravelMap:
    type: application
    platform: iOS
    configFiles:
      Debug: Config.xcconfig
      Release: Config.xcconfig
    sources:
      - path: TravelMap
        excludes:
          - "Resources/MapData/**"
          - "Resources/AppIcon.icon/**"
      - path: TravelMap/Resources/MapData
        type: folder
        buildPhase: resources
      # Icon Composer document — actool compiles it into the layered Liquid Glass icon.
      - path: TravelMap/Resources/AppIcon.icon
        type: file
        buildPhase: resources
    dependencies:
      - package: Supabase
        product: Supabase
    entitlements:
      path: TravelMap/TravelMap.entitlements
      properties:
        com.apple.developer.applesignin:
          - Default
    info:
      path: TravelMap/Info.plist
      properties:
        CFBundleDisplayName: TravelMap
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        UILaunchScreen:
          UIColorName: LaunchBackground
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        ITSAppUsesNonExemptEncryption: false
        SupabaseURL: $(SUPABASE_URL)
        SupabaseAnonKey: $(SUPABASE_ANON_KEY)
    scheme:
      testTargets: []
      gatherCoverageData: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.placeholder.travelmap
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        TARGETED_DEVICE_FAMILY: "1,2"
